spring:
  application:
    name: kafka-producer
  kafka:
    producer:
      bootstrap-servers:
        - localhost:9092
      properties:
        schema:
          registry:
            url: http://localhost:8081
