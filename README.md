This project includes the learning of apache-kafka and spring-boot configuration
How we can serialize/deserialize key and value while producing/consume the message 
Used the avaro-schema-registry 
An Avro Schema Registry is a centralized service used primarily in distributed systems like Apache Kafka to manage, store, and version Apache Avro schemas. 
It serves as a single source of truth for the data "contracts" shared between producers and consumers.
